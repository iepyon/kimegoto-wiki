# LLM Wiki — カードスキーマ定義

カードの型は次のとおり。フロー層は会議ごとに生成し、ストック層は昇格承認を経て会議をまたいで育つ。

<!-- generated:types — ontology.yaml から生成。手編集禁止 -->

| 層 | 型 | 名前 | ID 形式 | 何を表すか | 閉じた status |
|---|---|---|---|---|---|
| フロー | LOG | 会話ログ | `LOG-YYYYMMDD-NN` | 論点単位に切り出した逐語の束。すべての引用の照合先。会議ごとに新規 | — |
| フロー | AGD | 議題 | `AGD-NNN` | 会議をまたいで続く「決めたいこと」。DEC / Q / ACT の親。決着で閉じる | `決着` / `取り下げ` |
| フロー | DEC | 決定事項 | `DEC-NNN` | 会議で決まったこと。会議をまたいで更新される | `覆された` |
| フロー | Q | 未定事項 | `Q-NNN` | 決めるために足りないもの。解決で閉じる | `解決` / `取り下げ` |
| フロー | ACT | アクション | `ACT-NNN` | 会社間で約束したこと。完了で閉じる | `完了` / `取り下げ` |
| ストック | CON | 制約 | `CON-NNN` | 選択肢を削る環境の性質。昇格承認を経て1枚が育つ | — |
| ストック | ASM | 前提 | `ASM-NNN` | 成り立っていればよい仮定。崩れたら決定を見直す | — |
| ストック | TERM | 用語 | `TERM-NNN` | 案件の用語と表記揺れ。Pass 2 の正規化に還流する | — |

会議の ID は `MTG-YYYYMMDD`。

<!-- /generated -->

---

## ID 規約

形式は冒頭の表のとおり。
LOG だけ日付を含める（会議ごとに新規で、通し番号だと衝突管理が面倒になるため）。
他の7種は通し番号。採番は最大値+1。

---

## ディレクトリ構成

```
ontology.yaml                    # 型・フィールド・語彙の正本（キット共通）
projects/<slug>/                 # 1案件1ディレクトリ。.env の CURRENT_PROJECT で選ぶ
  role-mapping.yaml              # 役割 → 所属・社名・決定権（案件ごと）

  meetings/
    MTG-20260918/
      transcript.md              # 文字起こし原本（保管のみ・引用照合には使わない）
      segments.yaml              # Pass 1 の出力
      logs/
        LOG-20260918-01.md       # ← 引用の照合先はこれ
        LOG-20260918-02.md
      unknown-terms.yaml         # Pass 2 の出力（未知語リスト）
      extraction-notes.yaml      # Pass 3 の欠落ガードの理由
      promote-candidates.yaml    # Pass 4 の出力（承認後は破棄可）

  agenda/AGD-004.md
  decisions/DEC-014.md
  questions/Q-031.md
  actions/ACT-008.md
  constraints/CON-007.md
  assumptions/ASM-003.md
  terms/TERM-012.md

  views/                         # 生成物（kime views）。手で編集しない
```

**文字起こし原本は案件のディレクトリに置くが、カードではない。** 保管と監査のためだけに存在し、引用の照合先は LOG カードである（Pass 2 で正規化しているため、原本と文字列が一致しない）。

---

## LOG — 会話ログ

<!-- generated:fields LOG — ontology.yaml から生成。手編集禁止 -->

| フィールド | 意味 | 種類 | 必須 | 値 | 記入主体 |
|---|---|---|---|---|---|
| `id` | カードの ID。ファイル名と一致させる | ID | ○ | — | 自動 |
| `type` | 型を表す固定値 | 自由記述 | ○ | — | 自動 |
| `meeting` | この論点を切り出した会議 | 参照 | ○ | `MTG` | 自動 |
| `title` | 論点の見出し | 自由記述 | ○ | — | 自動 |
| `種別` | 論点の性質 | 語彙 | ○ | `議論` / `報告` / `確認` / `雑談` | 自動 |
| `時刻` | 文字起こし上の開始・終了時刻 | 時刻範囲 | — | — | 自動 |
| `参加役割` | この論点で発言した役割 | 文字列の配列 | ○ | — | 自動 |

<!-- /generated -->

論点単位に切り出した逐語の束。**すべての引用の照合先**であり、他のカードの親。

```yaml
---
id: LOG-20260918-03
type: log
meeting: MTG-20260918
title: 認証方式の選定
種別: 議論
時刻: "00:21:10 - 00:34:52"
参加役割: [顧客PM, 開発リーダ, 開発メンバー]
---

## 発言

- **顧客PM**: ADに繋ぐのは、まあVPNが固定IPなんで厳しいですね
- **開発リーダ**: だとするとOIDCに寄せる手もあります
- **開発リーダ**: 自前で作る手もあるんですけどね
- **顧客PM**: じゃあOIDCの方向でいきましょう
- **開発メンバー**: 利用者は当面50人くらいの想定でいいですか
- **顧客PM**: はい、当面はその規模です
```

本文の形式は `- **役割**: 発言` で固定。引用検証がこの行から発言部分を取る。

**種別が `報告` / `雑談` の LOG からは DEC を抽出しない**（Pass 3 でスキップする）。何も生まない LOG はそれでよい。後から意味を持つことがある。

---

## AGD — 議題

<!-- generated:fields AGD — ontology.yaml から生成。手編集禁止 -->

| フィールド | 意味 | 種類 | 必須 | 値 | 記入主体 |
|---|---|---|---|---|---|
| `id` | カードの ID。ファイル名と一致させる | ID | ○ | — | 自動 |
| `type` | 型を表す固定値 | 自由記述 | ○ | — | 自動 |
| `title` | 決めたいこと。提起者の言葉のまま | 自由記述 | ○ | — | 人間 |
| `status` | 議題の状態 | 語彙 | ○ | `未着手` / `継続` / `決着` / `取り下げ` | 人間 |
| `提起者` | 議題を出した役割 | 自由記述 | ○ | — | 人間 |
| `提起日` | 議題を出した日 | 日付 | ○ | — | 導出 |
| `予定会議` | 扱う会議。載った会議が足されていく | 文字列の配列 | — | — | 人間 |
| `決着日` | 決着・取り下げにした会議の日 | 日付 | — | — | 人間 |

<!-- /generated -->

```yaml
---
id: AGD-004
type: agenda
title: 退職者の権限をいつ止めるかを決めたい   # 提起者の言葉のまま
status: 継続
提起者: 顧客PM                 # 役割
提起日: 2026-09-11
予定会議: [MTG-20260918]       # 扱う会議。未来の会議でよい。載った会議が agenda-sync で足される
決着日:                        # 決着・取り下げにした会議の日（閉じるときに人が書く）
---
```

会議をまたいで続く「決めたいこと」の単位で、**DEC / Q / ACT の親**。
アジェンダは「その会議に載る議題と、その下にぶら下がるカード」の射影になる。

**子が親を指し、親は子の一覧を持たない。** DEC / Q / ACT の `議題` が AGD を1つ指し、
議題の下に何があるかは逆引きで出す（`wiki.children_of(agd)`）。両方向に書くと、
片方だけ直されて食い違う（カードに `meeting` を持たせないのと同じ理由）。

`議題` は人が付け替えるものではなく、**Pass 1 が論点に付けた議題を `kime new --from-log` が写す。**
論点と議題の対応は切り出しの時点でいちばんよく分かる。

**議題は決定ではない。** `title` は提起者が言ったとおりに書く。言い換えると
「何を決めたかったのか」が変わり、決着したかどうかの判定が狂う。
文字起こしより先に起票されるので引用を持たず、引用検証の対象にもならない。

載る会議は機械で決まる（`ontology.yaml` の `agenda`）。`決着` / `取り下げ` にしない限り
次回へ自動で持ち越される。閉じるのは人間（確認②）。

**結論が出なかった議題は、開いたままにすることで表す。** 議題を言い換えた Q は立てない
（二重になる）。Q に立てるのは「決めるために足りないもの」だけで、引用と確認先を持つので
「誰の何を待っているか」が残る。理由が発言に出なかったなら何も起票しない。

「扱った」かは論点（`segments.yaml` の `議題`）で決まり、`予定会議` との差で
アジェンダに「扱ったが結論なし」「扱えず」と出る。`未着手 → 継続` と `予定会議` の追記は
`kime agenda-sync` が書き戻す（一意に決まるので人に書かせない）。

---

## DEC — 決定事項

<!-- generated:fields DEC — ontology.yaml から生成。手編集禁止 -->

| フィールド | 意味 | 種類 | 必須 | 値 | 記入主体 |
|---|---|---|---|---|---|
| `id` | カードの ID。ファイル名と一致させる | ID | ○ | — | 自動 |
| `type` | 型を表す固定値 | 自由記述 | ○ | — | 自動 |
| `title` | 決まったことの要約 | 自由記述 | ○ | — | 自動 |
| `status` | 決定の状態 | 語彙 | ○ | `決定` / `保留` / `覆された` | 自動 |
| `決定日` | 決定した会議の日 | 日付 | ○ | — | 自動 |
| `決定の所在` | 決めた役割。覆せるのは誰かを表す | 自由記述 | ○ | — | 自動 |
| `会議体` | 決定した契約上の会議体 | 自由記述 | ○ | — | 導出 |
| `種別` | 今後変えられるか | 語彙 | ○ | `交渉可能` / `技術判断` / `契約制約` | 導出 |
| `引用` | 根拠となる発言の逐語 | 引用 | ○ | — | 自動 |
| `信頼度` | 引用の裏付けの強さ | 語彙 | ○ | `逐語あり` / `言及のみ` / `推測` | 自動 |
| `derived_from` | 根拠の LOG | 参照の配列 | ○ | `LOG` | 自動 |
| `スコープ` | 当初の合意に入っていたか | 語彙 | ○ | `当初スコープ内` / `スコープ外(追加)` / `判定保留` | 自動 |
| `議題` | 属する議題 | 参照 | — | `AGD` | 導出 |
| `同一性候補` | 同じものかもしれない既存のカード（Pass 3 が同一と確信できなかったときに付ける） | 参照の配列 | — | `DEC` / `Q` / `ACT` | 自動 |
| `確定日` | 顧客提出版を送り、異議なく確定した日 | 日付 | — | — | 人間 |
| `なぜ` | 決定の理由。人間が言った言葉だけ | 自由記述 | — | — | 人間 |
| `受容した不利` | 採った案の不利 | 自由記述 | — | — | 人間 |
| `確認方法` | この決定が守られているかの確かめ方 | 自由記述 | — | — | 人間 |
| `作らない` | この決定で作らないと決めたもの | 語彙 | — | `該当なし` / `記録なし` / `未記入` / 自由記述 | 人間 |
| `superseded_by` | この決定を覆した決定 | 参照 | — | `DEC` | 人間 |
| `代替案` | 検討して採らなかった案と却下理由 | 構造化配列 | — | `案` / `却下理由` / `引用` / `信頼度` | 自動 |

<!-- /generated -->

```yaml
---
id: DEC-014
type: decision
title: 認証を自社実装せず OIDC に寄せる
status: 決定
決定日: 2026-09-18

# 自動（role-mapping.yaml から導出）
決定の所在: 顧客PM             # 役割で持つ。社名に丸めない
会議体: 定例WG                 # 契約上の会議体名。役割だけでは契約と接続できない
種別: 交渉可能
引用: じゃあOIDCの方向でいきましょう
信頼度: 逐語あり
derived_from: [LOG-20260918-03]

# 受託開発では必ず見る欄（種別とは直交する軸）
スコープ: 判定保留             # 判定保留 のあいだ「スコープの確認」としてアジェンダと
                              # 議事録に出る（Q カードは作らない。射影で足りる）

# 顧客提出版の送付後に確定（みなし確定ルール）
確定日:                        # 異議なく確定した日

# 任意（人間が②の確認で記入。空欄のままでも起票できる）
なぜ:                          # Y-statement の穴埋め推奨（decision-guide.md 参照）
受容した不利:                   # 採った案の不利。却下理由とは別物
確認方法:                       # この決定が守られているかをどう確認するか
作らない: 未記入
superseded_by:                # 覆された場合に DEC-NNN

代替案:                        # WinWin の option と同一概念
  - 案: 顧客の既存ADに直結
    却下理由: 顧客側VPNが固定IPのみ
    引用: ADに繋ぐのは、まあVPNが固定IPなんで厳しいですね
    信頼度: 逐語あり
  - 案: 自社でID基盤を実装
    却下理由: 記録なし
    引用: 自前で作る手もあるんですけどね
    信頼度: 推測               # 沈黙由来の却下は 推測 固定（後述）
---

## 補足

（任意。人間が書く）
```

### `スコープ` を持つ理由（受託開発では最重要）

`種別` は「今後変えられるか」の軸、`スコープ` は「当初の合意に入っていたか」の軸。
別の問いなので両方要る。迷ったら `判定保留` にする。ここで無理に判定すると、後で追加請求の根拠を失う。

`判定保留` のあいだは、アジェンダと議事録に「スコープの確認」として射影される（`kime scope-questions`）。
Q カードは作らない。人が `スコープ` を書けばその時点で消えるので、閉じる操作も閉じ忘れの検査も要らない。

### 寿命管理の3点セット

| カード | フィールド | 検知するもの |
|---|---|---|
| DEC | `確認方法` | 決定が守られているか |
| ASM | `signpost` | 前提が崩れたか |
| CON | `失効条件` | 制約が消えたか |

`確認方法` は MADR 4.0.0 の任意項目 `Confirmation` に対応する。

### `なぜ` の書き方

自由記述にすると Notes 化して価値を失う
（Falessi ほか 2013 で13カテゴリ中 Notes だけが一貫して低価値）。
Y-statement の穴埋め形を雛形に使う。詳細は `decision-guide.md`。

```
〈どの場面〉で〈どの懸念〉に直面し、
〈採った案〉を選んだ。〈狙い〉のためであり、〈受容した不利〉を受け入れる。
```

### `なぜ` が未記入だと起きること

必須フィールドは人間に作話させるので使わない。代わりに書かないことに即時コストを付ける。

- 次回会議のアジェンダ冒頭に自動掲示される
- **CON / ASM への昇格候補になれない**（その制約が将来の議論で効かない）

### 決定の所在を役割で持つ理由

「覆せるのは誰か」が引けるようにするため。社名に丸めると `種別` の判定ができなくなる（顧客PMの判断は交渉可能、顧客法務の判断は契約制約）。

| 決定の所在 | 種別 | 半年後の扱い |
|---|---|---|
| 顧客PM / 発注元 | 交渉可能 | 再提案できる |
| 開発リーダ | 技術判断 | 前提が変われば覆せる |
| 顧客法務 / 契約 | 契約制約 | 覆せない |

### 信頼度の3値

| 値 | 意味 |
|---|---|
| `逐語あり` | 引用が LOG カード内に存在する（機械検証済み） |
| `言及のみ` | 代替案の言及の引用は取れたが、却下理由の発言がない |
| `推測` | 引用が検証できなかった、または**沈黙を根拠にした判定**。人間が確認するまで昇格させない |

`推測` は `verify_quotes.py` が自動で降格させる。LLM に自己申告させない。

**沈黙を根拠とする却下判定は無条件で `推測` に固定する。** 沈黙を却下のシグナルとして使う手法は
文献に対応するものがない。明示的な合意ですら人間の一致率が κ 0.63、自動検出が F1 0.39 で
最も難しいクラスである（Fernández ほか 2008）。沈黙はそれより弱い根拠でしかない。

---

## Q — 未定事項

<!-- generated:fields Q — ontology.yaml から生成。手編集禁止 -->

| フィールド | 意味 | 種類 | 必須 | 値 | 記入主体 |
|---|---|---|---|---|---|
| `id` | カードの ID。ファイル名と一致させる | ID | ○ | — | 自動 |
| `type` | 型を表す固定値 | 自由記述 | ○ | — | 自動 |
| `title` | 決めるために足りないこと | 自由記述 | ○ | — | 自動 |
| `status` | 未定事項の状態 | 語彙 | ○ | `未決` / `解決` / `取り下げ` | 自動 |
| `確認先` | 回答を待っている役割 | 自由記述 | ○ | — | 自動 |
| `対立当事者` | 意見が合っていない役割 | 文字列の配列 | — | — | 自動 |
| `初出` | 最初に出た会議の日 | 日付 | ○ | — | 自動 |
| `最終言及` | 最後に言及された会議の日 | 日付 | ○ | — | 自動 |
| `引用` | 根拠となる発言の逐語 | 引用 | ○ | — | 自動 |
| `信頼度` | 引用の裏付けの強さ | 語彙 | ○ | `逐語あり` / `言及のみ` / `推測` | 自動 |
| `derived_from` | 根拠の LOG | 参照の配列 | ○ | `LOG` | 自動 |
| `resolved_by` | この問いを解決した決定 | 参照 | — | `DEC` | 自動 |
| `議題` | 属する議題 | 参照 | — | `AGD` | 導出 |
| `同一性候補` | 同じものかもしれない既存のカード（Pass 3 が同一と確信できなかったときに付ける） | 参照の配列 | — | `DEC` / `Q` / `ACT` | 自動 |

<!-- /generated -->

```yaml
---
id: Q-031
type: question
title: 退職者の権限失効タイミングをどう保証するか
status: 未決
確認先: 顧客PM                 # 誰の回答を待っているか（役割）
対立当事者: [顧客PM, 開発リーダ] # 意見が合っていない当事者。空でもよい
初出: 2026-09-18
最終言及: 2026-09-18
引用: 退職した人の権限はいつ切れるんでしょうね
信頼度: 逐語あり
derived_from: [LOG-20260918-03]
resolved_by:                  # 解決時に DEC-NNN
議題: AGD-004                  # どの議題の中の未決か（論点から導出。無ければ空）
---
```

閉じていない（冒頭の型一覧の「閉じた status」でない）ものは、次回定例のアジェンダに自動で積む。

`対立当事者` は WinWin（Boehm ほか 1998）の **issue** の考え方から。
Q を「不明点」一般ではなく**利害の衝突**として起票すると、確認先が自動で決まる。
衝突ではない単純な確認（仕様の穴）は空欄でよい。

**「前向きに検討します」「持ち帰ります」「調整します」は DEC ではなく必ず Q。**
これらを DEC にすると、半年後に「合意したはずだ」という水掛け論の材料になる。

---

## ACT — アクション

<!-- generated:fields ACT — ontology.yaml から生成。手編集禁止 -->

| フィールド | 意味 | 種類 | 必須 | 値 | 記入主体 |
|---|---|---|---|---|---|
| `id` | カードの ID。ファイル名と一致させる | ID | ○ | — | 自動 |
| `type` | 型を表す固定値 | 自由記述 | ○ | — | 自動 |
| `title` | やること | 自由記述 | ○ | — | 自動 |
| `担当` | 担う会社（社名） | 自由記述 | — | — | 人間 |
| `期限` | 期日 | 日付 | — | — | 人間 |
| `status` | アクションの状態 | 語彙 | ○ | `未着手` / `進行中` / `完了` / `遅延` / `取り下げ` | 人間 |
| `対策種別` | 前提（ASM）への働きかけの種類 | 語彙 | — | `shaping` / `hedging` | 自動 |
| `引用` | 根拠となる発言の逐語 | 引用 | ○ | — | 自動 |
| `信頼度` | 引用の裏付けの強さ | 語彙 | ○ | `逐語あり` / `言及のみ` / `推測` | 自動 |
| `derived_from` | 根拠の LOG | 参照の配列 | ○ | `LOG` | 自動 |
| `関連` | 関係するカード | 参照の配列 | — | `DEC` / `Q` / `CON` / `ASM` | 自動 |
| `更新履歴` | 日付ごとの状態の変化 | 構造化配列 | — |  | 人間 |
| `issue` | 起票した GitHub Issue | 自由記述 | — | — | 導出 |
| `議題` | 属する議題 | 参照 | — | `AGD` | 導出 |
| `同一性候補` | 同じものかもしれない既存のカード（Pass 3 が同一と確信できなかったときに付ける） | 参照の配列 | — | `DEC` / `Q` / `ACT` | 自動 |

<!-- /generated -->

**担当は社名で持つ。** 会社間の約束を記録するのがこのカードの役割であり、社内の誰がやるかはタスク管理側の仕事。

```yaml
---
id: ACT-008
type: action
title: IdP の利用可否を情報システム部門に確認する
担当: 〇〇株式会社              # 社名。role-mapping.yaml から導出。空欄を許す
期限: 2026-10-02              # 人間が記入。空欄を許す
status: 未着手
対策種別:                      # ASM から生えたアクションのみ記入
引用: こちらで情シスに確認しておきます
信頼度: 逐語あり
derived_from: [LOG-20260918-03]
関連: [Q-031, ASM-003]
更新履歴:
  - 2026-09-18: 起票
---
```

status の更新は**定例会議でのみ**行う。相手社のアクションの完了を自社では判定できないため。
閉じていない（冒頭の型一覧の「閉じた status」でない）ものは、次回定例のアジェンダに自動で積む。

### `担当` と `期限` は空欄を許す

アクション抽出の研究で、クラス別の難易度は一貫して
**本文 > 担当 > 期限**の順（Purver ほか 2007: Description 0.19 / Timeframe 0.22 / Owner 0.24、
人間の一致率も κ 0.435–0.78）。

LLM に自由記述で埋めさせると捏造する。**空欄が正常な出力**であるとプロンプトに明示し、
②の確認で人間が埋める。

### `対策種別`（ASM から生えたアクションのみ）

Assumption-Based Planning の区別。
- `shaping` — 前提が崩れないように働きかける
- `hedging` — 崩れた場合に備える

どちらでもないアクションは空欄。

---

## CON — 制約

<!-- generated:fields CON — ontology.yaml から生成。手編集禁止 -->

| フィールド | 意味 | 種類 | 必須 | 値 | 記入主体 |
|---|---|---|---|---|---|
| `id` | カードの ID。ファイル名と一致させる | ID | ○ | — | 自動 |
| `type` | 型を表す固定値 | 自由記述 | ○ | — | 自動 |
| `内容` | 制約の内容 | 自由記述 | ○ | — | 自動 |
| `種類` | 環境の事実か、相手方への期待か | 語彙 | ○ | `property` / `expectation` | 自動 |
| `所在` | 制約を持つ側 | 語彙 | ○ | `自社側` / `顧客側` / `協力会社` / `契約` | 導出 |
| `硬度` | どれだけ覆しにくいか | 語彙 | ○ | `溶液` / `懸濁` / `沈殿` / `岩盤` | 導出 |
| `失効条件` | 制約が消える条件 | 自由記述 | — | — | 自動 |
| `影響する決定` | この制約が効く決定 | 参照の配列 | — | `DEC` | 自動 |
| `derived_from` | 根拠の LOG | 参照の配列 | ○ | `LOG` | 自動 |
| `引用` | 根拠となる発言の逐語 | 引用 | — | — | 自動 |
| `信頼度` | 引用の裏付けの強さ | 語彙 | — | `逐語あり` / `言及のみ` / `推測` | 自動 |
| `承認` | 昇格を承認した日 | 日付 | ○ | — | 人間 |
| `status` | 制約の状態 | 語彙 | ○ | `有効` / `失効` | 人間 |

<!-- /generated -->

選択肢を削る環境の性質。**却下理由から昇格する**。Pass 4 の承認を経たものだけが存在する。

```yaml
---
id: CON-007
type: constraint
内容: 顧客側VPNは固定IPのみ
種類: expectation
所在: 顧客側
硬度: 沈殿                     # 種類 × 所在 から導出
失効条件: 顧客のネットワーク更新（2027年度予定と聞いている）
影響する決定: [DEC-014]
derived_from: [LOG-20260918-03]
承認: 2026-09-18
status: 有効
---
```

### `種類` — property と expectation は別物だった

KAOS（要求工学）は環境側の記述を分けている。

| 種類 | KAOS の用語 | 例 | 失効条件 |
|---|---|---|---|
| `property` | domain property — 環境についての事実 | 法令、物理法則、既存システムの仕様 | **空でよい**（事実は失効しない） |
| `expectation` | expectation — 相手方に割り当てた規範的言明 | 「顧客が月次で利用者数を報告してくれる」 | **必要**（破られ得る） |

以前はこの2つを同じ `失効条件` 必須のフィールドで扱っていたため、
事実に対して埋まらない欄が発生していた。

### 判定テスト — 自社で守れるものは CON ではない

```
その制約は、自社の作業で守れるか？
├ 守れる                   → CON でも ASM でもない。ACT か DEC
└ 守れない
   ├ 環境の事実           → CON（種類: property）
   ├ 相手方への期待       → CON（種類: expectation）
   └ 成り立っていればよい仮定 → ASM
```

根拠：「**assumptions cannot be enforced by the software-to-be**」（van Lamsweerde 2001）。
この一文がそのまま判定テストになる。

### 硬度の導出

硬度は入力せず、`種類` と `所在` から機械的に決める。

<!-- generated:derivation 硬度 — ontology.yaml から生成。手編集禁止 -->

| 種類 | 所在 | 硬度 |
|---|---|---|
| property | 契約 | 岩盤 |
| property | 顧客側 | 沈殿 |
| property | 自社側 | 沈殿 |
| property | 協力会社 | 沈殿 |
| expectation | 顧客側 | 沈殿 |
| expectation | 自社側 | 懸濁 |
| expectation | 協力会社 | 懸濁 |

<!-- /generated -->

### 昇格の門

その決定に固有か、環境の性質か。

- 「この案は工数が合わない」→ 固有。昇格しない
- 「顧客側VPNが固定IPのみ」→ 環境の性質。昇格する

**迷ったら昇格させない。** 同じ理由が2つ目の決定に現れた時点で昇格すればよい。

---

## ASM — 前提

<!-- generated:fields ASM — ontology.yaml から生成。手編集禁止 -->

| フィールド | 意味 | 種類 | 必須 | 値 | 記入主体 |
|---|---|---|---|---|---|
| `id` | カードの ID。ファイル名と一致させる | ID | ○ | — | 自動 |
| `type` | 型を表す固定値 | 自由記述 | ○ | — | 自動 |
| `内容` | 前提の内容 | 自由記述 | ○ | — | 自動 |
| `崩れたら見直す決定` | 前提が崩れたときに見直す決定 | 参照の配列 | — | `DEC` | 自動 |
| `脆弱性` | 覆されやすさ | 語彙 | ○ | `高` / `中` / `低` | 自動 |
| `signpost` | 前提が崩れたと分かる事象または閾値 | 自由記述 | — | — | 自動 |
| `次回確認日` | 次に確かめる日 | 日付 | — | — | 人間 |
| `引用` | 根拠となる発言の逐語 | 引用 | ○ | — | 自動 |
| `信頼度` | 引用の裏付けの強さ | 語彙 | ○ | `逐語あり` / `言及のみ` / `推測` | 自動 |
| `derived_from` | 根拠の LOG | 参照の配列 | ○ | `LOG` | 自動 |
| `最終確認` | 最後に確かめた日 | 日付 | ○ | — | 人間 |
| `status` | 前提の状態 | 語彙 | ○ | `有効` / `崩れた` / `確認中` | 人間 |

<!-- /generated -->

**逆リンクが本体。** 内容ではなく「崩れたら見直す決定」のリストが価値。

```yaml
---
id: ASM-003
type: assumption
内容: 当面の利用者は50人規模
崩れたら見直す決定: [DEC-014, DEC-019]      # ← これが本体（load-bearing）
脆弱性: 高                     # vulnerable の軸
signpost: 月次報告の利用者数が80人を超える   # 事象または閾値。「注意して見る」は不可
次回確認日: 2026-10-02          # 既定値＝次回会議日
引用: はい、当面はその規模です
信頼度: 逐語あり
derived_from: [LOG-20260918-03]
最終確認: 2026-09-18
status: 有効
---
```

制約は破ろうとした時に気づくが、**前提は誰にも気づかれずに崩れる**。
逆リンクと signpost がないと、崩れた前提に基づく決定が残り続ける。

### `signpost` — Assumption-Based Planning の用語

「検知方法」と呼んでいたものには標準名がある。**signpost**の定義は

> **an event or threshold that, if detected, signifies that a vulnerable assumption is being broken**
> （Walker, Haasnoot & Kwakkel 2013 / Assumption-Based Planning, RAND）

**事象または閾値**でなければならない。「利用者数に注意する」は signpost ではない。
「月次報告の利用者数が80人を超える」が signpost。閾値が書けない前提は、追跡できない。

### `脆弱性` — 全 ASM を追跡しない門

ABP は前提を2軸で絞る。

- **load-bearing** — 計画がそれに依存している → 我々の `崩れたら見直す決定` が表現している
- **vulnerable** — 覆されやすい → **この軸が欠けていた**

**運用対象は `脆弱性: 高` かつ `崩れたら見直す決定` が空でないものだけ。**
それ以外は記録するが棚卸しの対象にしない。全件追跡は2週25分では破綻する。

`崩れたら見直す決定` が特定できない前提は、Pass 4 の候補にも出さない。

### 検出トリガー

決定の理由に次の語が付いていたら必ず前提を切る。

<!-- generated:words assumption-trigger-words — ontology.yaml から生成。手編集禁止 -->

```
今は / 当面 / のうちは / 現状 / いまのところ / 今回は / 初期は / とりあえず / 一旦 / 今のフェーズ / 規模なので
```

<!-- /generated -->

### ASM から生えるアクション

ABP は対策を2種に分ける。ACT の `対策種別` に入れる。

- `shaping` — 前提が崩れないように働きかける
- `hedging` — 崩れた場合に備える

### 棚卸し運用

四半期ごと（年4回）、定例会議で `脆弱性: 高` の前提だけを確認する。5分で済む。
これを入れないと前提カードは飾りになる。

---

## TERM — 用語

<!-- generated:fields TERM — ontology.yaml から生成。手編集禁止 -->

| フィールド | 意味 | 種類 | 必須 | 値 | 記入主体 |
|---|---|---|---|---|---|
| `id` | カードの ID。ファイル名と一致させる | ID | ○ | — | 自動 |
| `type` | 型を表す固定値 | 自由記述 | ○ | — | 自動 |
| `正式` | 正式な表記 | 自由記述 | ○ | — | 自動 |
| `読み` | 読み方 | 自由記述 | — | — | 人間 |
| `表記揺れ` | 文字起こしに実際に出た誤表記 | 文字列の配列 | — | — | 自動 |
| `定義` | この案件での意味 | 自由記述 | — | — | 人間 |
| `初出` | 最初に出た LOG | 参照 | — | `LOG` | 自動 |
| `意味が確定した決定` | 意味を確定させた決定 | 参照 | — | `DEC` | 自動 |

<!-- /generated -->

**8種のうち唯一、溜まると入力品質が上がる型。** `表記揺れ` が Pass 2 の正規化に還流する。

```yaml
---
id: TERM-012
type: term
正式: OIDC
読み: オーアイディーシー
表記揺れ: [オイディーシー, オイデッシー, オープンIDコネクト, OpenIDConnect, オープンアイディーコネクト]
定義: OpenID Connect。本PJでは認証をこれに寄せる
初出: LOG-20260918-03
意味が確定した決定: DEC-014
---
```

`表記揺れ` は文字起こしが実際に間違えた表記を蓄積する。想像で埋めず、出てきたものを足していく。

### 昇格の判定基準

- カタカナ語・英字略語・不自然な漢字列に限る
- 同一会議内で `thresholds.term-min-occurrences` 回以上出現
- 複数回の会議にまたがって出る未知語は確定で用語

---

## 周辺ファイル

カードではないが、機械とスキルが読み書きする YAML。書式の正本は `ontology.yaml` の `files`。

<!-- generated:files — ontology.yaml から生成。手編集禁止 -->

### `role-mapping.yaml`

役割 → 所属・社名・決定権。案件ごとに持ち、キットのルートにフォールバックしない

| キー | 場所 | 意味 | 値 |
|---|---|---|---|
| `meta` | 最上位 | 案件の情報 | — |
| `project` | `meta` | 案件名 | — |
| `自社` | `meta` | 自社の社名 | — |
| `顧客` | `meta` | 顧客の社名 | — |
| `協力会社` | `meta` | 協力会社の社名の配列 | — |
| `会議体` | `meta` | 契約上の会議体名。DEC の `会議体` はここから導出する | — |
| `roles` | 最上位 | 役割名 → 役割の属性 | — |
| `unknown_role_default` | 最上位 | 未登録の役割に当てる属性 | — |
| `所属` | `roles.*` | 役割の側（CON の `所在` はここから導出する） | — |
| `社名` | `roles.*` | 役割の会社名（ACT の `担当` はここから導出する） | — |
| `決定権` | `roles.*` | この役割の発言で DEC を確定してよいか | `あり` / `なし` |
| `決定の種別` | `roles.*` | この役割が決めたことの種別（DEC の `種別` はここから導出する） | `交渉可能` / `技術判断` / `契約制約` |

### `meetings/MTG-*/segments.yaml`

Pass 1 の出力。論点の切り出し

| キー | 場所 | 意味 | 値 |
|---|---|---|---|
| `meeting` | 最上位 | 会議の ID | — |
| `segments` | 最上位 | 論点の配列 | — |
| `seq` | `segments[]` | 論点の通し番号（1から連番） | — |
| `title` | `segments[]` | 論点の見出し | — |
| `種別` | `segments[]` | 論点の性質 | `議論` / `報告` / `確認` / `雑談` |
| `時刻` | `segments[]` | 文字起こし上の開始・終了時刻 | — |
| `参加役割` | `segments[]` | この論点で発言した役割 | — |
| `議題` | `segments[]` | 論点が扱った議題（AGD） | — |

### `meetings/MTG-*/extraction-notes.yaml`

Pass 3 の欠落ガードの理由。何も起こさなかった論点の説明

| キー | 場所 | 意味 | 値 |
|---|---|---|---|
| `meeting` | 最上位 | 会議の ID | — |
| `notes` | 最上位 | LOG ごとの記録の配列 | — |
| `log` | `notes[]` | 対象の LOG | — |
| `extraction_empty_reason` | `notes[]` | 何も起こさなかった理由（1行） | — |
| `review_required` | `notes[]` | 判断がついていない印（true なら lint が鳴らし続ける） | — |

### `meetings/MTG-*/unknown-terms.yaml`

Pass 2 の出力。未知語の候補（`kime unknown-terms` が数える）

| キー | 場所 | 意味 | 値 |
|---|---|---|---|
| `meeting` | 最上位 | 会議の ID | — |
| `unknown_terms` | 最上位 | 未知語の配列 | — |
| `語` | `unknown_terms[]` | 文字起こしに出た表記 | — |
| `出現回数` | `unknown_terms[]` | この会議での出現回数 | — |
| `初出` | `unknown_terms[]` | 最初に出た LOG | — |
| `文脈` | `unknown_terms[]` | 最初に出た発言 | — |
| `推定` | `unknown_terms[]` | 何の表記崩れかの推定 | — |

### `meetings/MTG-*/promote-candidates.yaml`

Pass 4 の出力。制約・前提・用語の昇格候補（承認後は破棄してよい）

| キー | 場所 | 意味 | 値 |
|---|---|---|---|
| `meeting` | 最上位 | 会議の ID | — |
| `candidates` | 最上位 | 候補の配列 | — |
| `summary` | 最上位 | 候補の種別ごとの件数 | — |
| `種別` | `candidates[]` | 候補の種別 | `新規制約` / `既存制約への紐づけ` / `新規前提` / `表記揺れの追記` / `新規用語` |
| `内容` | `candidates[]` | 制約・前提の内容 | — |
| `種類` | `candidates[]` | 制約の種類 | `property` / `expectation` |
| `根拠` | `candidates[]` | 候補の出どころ（どの決定のどの欄か） | — |
| `引用` | `candidates[]` | 根拠となる発言の逐語 | — |
| `derived_from` | `candidates[]` | 根拠の LOG | — |
| `失効条件` | `candidates[]` | 制約が消える条件 | — |
| `影響しそうな既存の決定` | `candidates[]` | 制約が効きそうな既存の DEC | — |
| `崩れたら見直す決定` | `candidates[]` | 前提が崩れたときに見直す DEC | — |
| `脆弱性` | `candidates[]` | 前提の覆されやすさ | `高` / `中` / `低` |
| `signpost` | `candidates[]` | 前提が崩れたと分かる事象または閾値 | — |
| `対象` | `candidates[]` | 表記揺れを足す TERM | — |
| `追加する表記` | `candidates[]` | 足す表記揺れ | — |
| `出現回数` | `candidates[]` | この会議での出現回数 | — |
| `正式` | `candidates[]` | 用語の正式な表記 | — |
| `定義案` | `candidates[]` | 会議で語られた定義 | — |
| `初出` | `candidates[]` | 最初に出た LOG | — |

<!-- /generated -->

---

## フィールドの記入主体まとめ

人間が触るのは次の4つだけ。

| | 内容 | 目安 |
|---|---|---|
| **人間** | DEC の `なぜ` / `作らない` / `代替案` の `却下理由` | 10分 |
| **人間** | ACT の `期限` / `status` | 5分 |
| **人間** | AGD の起票（文言・提起者）と、決着したかの判定 | 会議の前後に数分 |
| **人間** | CON / ASM / TERM の昇格承認（yes/no） | 10分 |
| 自動 | 上記以外すべて | — |

2週に1回・計25分。年間5.5時間。
