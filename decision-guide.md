# 判定ガイド — 何を DEC とするか

Ahmeti ほか (ECSA 2024) のアクションリサーチで報告された最大の障害は、
記述コストではなく**「これは記録に値するか」の判定**だった。
ADR 自体は "really easy to write" と評価されている。
そして**効いた唯一の介入がガイドラインの明示**（満足度 2.0→2.9 / 2.5→3.1）。

だからこの1枚を先に作る。25分の人手は「書く」ではなく**判定**に使う。

---

## 大原則

> **LLM には過剰に出させる。人間は捨てる作業だけをする。**

決定検出は再現 0.88 / 精度 0.43 まで届く（Fernández 2008）。
つまり「拾いすぎて人が捨てる」形が、機械の得意な形と一致している。
逆向き（精度を上げて人が足す）は機械も人間も苦手。

---

## DEC にする

次のいずれかを満たすもの。

- [ ] **選択肢が2つ以上あって、1つが選ばれた**（もう一方は `代替案` になる）
- [ ] **やらないことが確定した**（`作らない` に入る）
- [ ] **スコープ・スケジュール・コストのいずれかが変わった**
- [ ] **顧客と自社のあいだで合意が成立した**（相手方の確認が必要なもの）
- [ ] **後から「なんでこうなってるの」と聞かれる可能性がある**

## DEC にしない

- [ ] **通常作業の進め方**（「じゃあ先にAPIから作ります」— 順序の話で、覆っても誰も困らない）
- [ ] **作業の分担**（→ `ACT`）
- [ ] **既に決まっていたことの確認**（→ LOG のみ。ただし`確定日`の記録対象になりうる）
- [ ] **決定権のない役割の発言だけで成立しているもの**（→ 必ず `Q`）
- [ ] **引用できる発話がないもの**（→ 起票しない）

---

## 境界例

実際に迷うのはこの辺なので、先に答えを決めておく。

| 発話 | 判定 | 理由 |
|---|---|---|
| 「ライブラリは X を使います」（開発リーダ） | **DEC**（種別: 技術判断） | 後から「なぜ X か」を聞かれる |
| 「先に認証から実装します」（開発リーダ） | **LOG のみ** | 作業順序。覆っても誰も困らない |
| 「じゃあ画面は10本で」（顧客PM） | **DEC**（スコープ: 判定保留） | 当初合意の本数と違えば追加交渉の対象 |
| 「そのあたりは任せます」（顧客PM） | **DEC**（種別: 交渉可能、決定の所在: 顧客PM） | 判断の委任そのものが決定 |
| 「持ち帰って確認します」（顧客担当者） | **Q + ACT** | Q（未決）と ACT（確認する約束）の両方 |
| 「前向きに検討します」（顧客PM） | **Q** | 決定ではない。日本語の曖昧表現は Q に落とす |
| 「それは厳しいですね」（顧客PM） | **CON 候補**（Pass 4 経由） | 制約の発見。DEC ではない |
| 「今は50人規模なので不要です」（顧客PM） | **DEC + ASM 候補** | 決定と、その条件（前提）の両方 |
| 「一旦置いといて」（顧客PM） | **Q**（status: 未決） | 保留。再開の可能性がある |
| 「一旦置いといて」（開発メンバー） | **LOG のみ** | 提案の取り下げ。決定権がない |

**「前向きに検討します」「持ち帰ります」「調整します」は決定ではない。** 必ず Q。
これらを DEC にすると、半年後に「合意したはずだ」という水掛け論の材料になる。

---

## スコープの判定（受託開発では最重要）

`スコープ: 当初スコープ内 / スコープ外(追加) / 判定保留`

これは `種別`（交渉可能 / 技術判断 / 契約制約）とは**直交する軸**。
「今後変えられるか」と「当初の合意に入っていたか」は別の問いである。

判定基準：

- **当初スコープ内** — 要件定義書・見積根拠に明記されている
- **スコープ外(追加)** — 明記されておらず、工数が増える
- **判定保留** — どちらとも言えない → Q は立てない。アジェンダと議事録に「スコープの確認」として出る（`kime scope-questions`）

迷ったら **`判定保留`**。ここで無理に判定すると、後で追加請求の根拠を失う。
弁護士法人クラフトマンの解説は、課題ごとにスコープの内外を**都度明確にしていく**ことを
有益な実務としている（凍結仕様書からの逸脱が明らかな場合に追加請求が認められやすい）。

**25分の中で人間が必ず目を通すのはこの欄。** 他は飛ばしてよい。

---

## CON / ASM / ACT の切り分け

`Pass 4` の昇格判定で使う。判定テストは1つで足りる。

```
その制約は、自社の作業で守れるか？
├ 守れる                   → CON でも ASM でもない。ACT か DEC
└ 守れない
   ├ 環境の事実（法令・物理・既存システム）
   │    → CON（種類: property）／失効条件は空でよい
   ├ 相手方に割り当てた期待（顧客が〜してくれる）
   │    → CON（種類: expectation）／失効条件が必要
   └ 単に成り立っていればよい仮定（利用者は50人規模）
        → ASM（脆弱性と signpost が必要）
```

根拠：KAOS の domain property / domain hypothesis / expectation の区別。
「**assumptions cannot be enforced by the software-to-be**」（van Lamsweerde 2001）が
そのまま判定テストになる。

---

## 昇格の門（CON / ASM）

Pass 4 の候補を全部通すと、2週25分では破綻する。門を2つ置く。

**CON の門** — その決定に固有か、環境の性質か。
- 「この案は工数が合わない」→ 固有。昇格しない
- 「顧客側VPNが固定IPのみ」→ 環境の性質。昇格

迷ったら昇格させない。同じ理由が2つ目の決定に現れた時点で昇格すればよい。

**ASM の門** — `脆弱性: 高` かつ `崩れたら見直す決定` が特定できているものだけ。
- Assumption-Based Planning は前提を **load-bearing**（計画がそれに依存）と
  **vulnerable**（覆されやすい）の2軸で絞る
- 逆リンクのない前提カードは飾りになる。候補にも出さない

---

## なぜを書かないことのコスト

必須フィールドは人間に作話させるので使わない。代わりに、書かないことに即時のコストを付ける。

- `なぜ` が未記入の DEC は、**次回会議のアジェンダ冒頭に自動掲示される**
- `なぜ` も `代替案[].却下理由` も無い DEC は、**CON / ASM への昇格候補になれない**
  （＝その制約が将来の議論で自動的に効かない）。
  却下理由が記録されていれば通る（CON の中身は捨てた案の理由なので、`なぜ` とは別物）

「書かないと次の会議が長くなる」という形にする。

根拠：Lee (1997) は「コスト負担者が受益者と同一でないとき、費用対効果のあるシステムを
提供することは一層困難になる」とし、処方の1つにインセンティブを挙げている。

---

## なぜの書き方（Y-statement）

自由記述にすると Notes 化して価値を失う
（Falessi ほか 2013: 13カテゴリのうち **Notes は一貫して低価値**）。
最小の構造として Olaf Zimmermann の Y-statement を雛形に使う。

> **In the context of** `<use case>`, **facing** `<concern>`
> **we decided for** `<option>` **to achieve** `<quality>`,
> **accepting** `<downside>`.

日本語の穴埋め形：

```
〈どの場面〉で〈どの懸念〉に直面し、
〈採った案〉を選んだ。〈狙い〉のためであり、〈受容した不利〉を受け入れる。
```

例：

> 認証方式の選定で、退職者の権限失効を自前で保証できないという懸念に直面し、
> OIDC 連携を選んだ。失効保証を IdP 側に寄せるためであり、
> IdP 障害時に全体が止まることを受け入れる。

最後の「受容した不利」が `受容した不利` フィールドに入る。
これは `代替案[].却下理由`（捨てた案の理由）とは別物。

**全部書けなくてよい。** 1つでも埋まれば、空欄より価値がある。
