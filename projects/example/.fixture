このディレクトリは教材であって、実運用の案件ではない。

DEC-014 の代替案2件目の引用は、LOG カードと意図的に1文字だけ違えてある
（verify_quotes.py が不一致を検出する様子を見せるため）。したがってこの案件は
lint の error を必ず1件出す。それが正しい状態である。

このファイルがあるディレクトリは、pre-commit と Stop フックの自動チェックから
外れる。明示的に指定したときだけ検査される:

    python3 tools/giji.py lint --root projects/example
